apiVersion: apps/v1
kind: Deployment
metadata:
  name: mwalika-agent-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mwalika-agent-production
  template:
    metadata:
      labels:
        app: mwalika-agent-production
    spec:
      containers:
        - name: mwalika-agent
          imagePullPolicy: IfNotPresent
          image: ${IMAGE_REF}
          ports:
            - containerPort: 3001
          envFrom:
            - secretRef:
                name: mwalika-agent-env-production
          resources:
            requests:
              cpu: "100m"         # 0.1 vCPU guaranteed
              memory: "128Mi"     # 128 MiB guaranteed
            limits:
              cpu: "500m"         # Max 0.5 vCPU allowed
              memory: "512Mi"     # Max 512 MiB allowed
          livenessProbe:
            httpGet:
              path: /api/system/health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 3
      imagePullSecrets:
        - name: regcred-mwalika
